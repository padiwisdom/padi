#include <iostream>

using namespace std;

int main()
{
    int num; n, coun=0;
    cout <<" Enter a number which has only two factors meaning it is a prime or not prime ";
    cin >> num;
    if ( num ==0 )
    {
        cout << num <<" is not prime";
    }
    else
    for( n =2; n<num; n++)
    if(num % n ==0)
    count ++
}
if (cout<< num <<" is not prime")
else
cout << num <<"is prime";
    return 0;
}
